services:
  api:
    build: ./api
    ports:
      - "8080:8080"

  simulator:
    build: ./simulator
    depends_on:
      - api

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - api
